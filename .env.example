# ===========================================
# Research Bot Configuration
# ===========================================

# Required: Telegram Bot Token from @BotFather
BOT_TOKEN=Insert:token

# ===========================================
# Optional Configuration
# ===========================================

# Database URL (SQLite by default)
DATABASE_URL="file:./sqlite.db"

# Redis URL for session persistence (Upstash Redis)
#
# You can use either format from Upstash dashboard:
# 1. Redis protocol URL (recommended - from "Connect" > "Node.js" > "ioredis"):
#    REDIS_URL=rediss://default:YOUR_TOKEN@your-endpoint.upstash.io:6379
#
# 2. REST API URL (from "Connect" > "REST API"):
#    REDIS_URL=https://your-endpoint.upstash.io
#    (Note: Token must be included in URL as password)
#
# Leave empty to use in-memory sessions (not recommended for production)
REDIS_URL=

# Webhook secret for verifying Telegram webhook requests
# Generate with: openssl rand -hex 32
WEBHOOK_SECRET=

# Server port for webhook mode (default: 3000)
PORT=3000

# Environment (development or production)
NODE_ENV=development

# Log level (debug, info, warn, error)
LOG_LEVEL=info

# ===========================================
# Admin Configuration (Optional)
# ===========================================

# Comma-separated list of Telegram user IDs for admin commands
# Example: ADMIN_IDS=123456789,987654321
ADMIN_IDS=
